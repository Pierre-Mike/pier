# Ideas

Running list of UX improvements. Not specs — just seeds. Promote to `specs/active/NNN-slug/` via `/do` when ready.

## Project sidebar (left)

- **Right-click → open GitHub page** for the project. Context menu item that resolves the repo's GitHub URL and opens it in the default browser.

## Artifacts panel (right)

- **Show existing worktrees alongside branches.** Worktrees and branches should look similar in the UI (same row style, same affordances) with a small visual differentiator — icon or color tint — so a worktree is recognizable at a glance without becoming a separate section.
